---
title: "Enhanced options for notification default settings customisation"
---

Admins now have more options to customise the default settings for notifications. Previously admins could only set default notifications to other "All notifications" or "Only my tasks".

Now, Admins can set defaults with the same level of granularity as normal notifications.